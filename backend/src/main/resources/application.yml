spring:
  application:
    name: university-exam-platform
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  datasource:
    url: jdbc:postgresql://postgres:5432/exam_platform
    username: exam_user
    password: exam_password_secure_123
    driver-class-name: org.postgresql.Driver

server:
  port: 8080
  servlet:
    context-path: /api